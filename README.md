# Rapid Hype - React + Vite (GSAP)

Quick scaffold of the Rapid Hype site converted to React using Vite and GSAP for animations.

Getting started:

1. Install dependencies

```bash
cd react-app
npm install
```

2. Run dev server

```bash
npm run dev
```

The app references the existing `assets` folder at the workspace root: `/assets`.
